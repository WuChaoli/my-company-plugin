---
name: context-engineering
description: 使用上下文工程系统管理开发上下文。当处理项目文档、开始新功能、组织开发工作、归档已完成工作，或用户提到"context management"、"开始新需求"、"归档上下文"、"查看活跃上下文"，或需要按功能和时间组织开发文档时使用。提供初始化上下文、跟踪活跃工作和归档已完成功能的结构化工作流。
---

# 上下文工程 (Context Engineering)

使用结构化文档系统管理开发上下文，按时间和功能组织工作，支持并行开发，并在静态文档和动态文档之间保持清晰的分离。

## 核心能力

1. **初始化新上下文** - 创建包含元数据和初始文档的结构化目录
2. **列出活跃上下文** - 查看所有正在进行的开发工作
3. **归档已完成上下文** - 标记工作为已完成并生成摘要
4. **维护文档结构** - 确保符合项目标准

## 何时使用此技能

在以下情况下使用此技能：
- 开始新功能或需求
- 需要查看当前活跃的开发工作
- 完成功能并想要归档上下文
- 处理遵循上下文工程模式的项目文档
- 用户说"开始新需求"、"查看活跃上下文"、"归档上下文"或提到上下文管理

## 工作流决策树

```
用户请求
    │
    ├─ "开始新功能" / "开始新需求"
    │   └─> 初始化新上下文
    │
    ├─ "我们在做什么？" / "查看活跃上下文"
    │   └─> 列出活跃上下文
    │
    ├─ "功能完成了" / "归档上下文"
    │   └─> 归档上下文
    │
    └─ "需要理解系统"
        └─> 阅读规范文档
```

## 初始化新上下文

创建一个包含完整目录结构、元数据和初始文档的新开发上下文。

### 使用脚本

```bash
python scripts/init_context.py <feature-name> [--assignee <name>] [--branch <branch-name>]
```

**示例：**
```bash
python scripts/init_context.py user-authentication --assignee developer --branch feature/user-auth
```

### 创建的内容

脚本会创建：
1. **上下文目录**：`docs/contexts/YYYY-MM-DD_feature-name/`
2. **元数据文件**：`.context.json`，包含状态、时间戳、负责人等
3. **初始文档**（功能级）：
   - `requirements.md` - 需求文档
   - `architecture-changes.md` - 架构变更
   - `feature-spec.md` - 功能规格
   - `plan.md` - 实施计划
   - `test-plan.md` - 测试计划
   - `todos.md` - 任务清单（可选）
4. **索引更新**：将上下文添加到 `.contexts-index.json`

**注意**：所有职位相关文档直接放在特性根目录，无需创建子目录层级。

### 上下文 ID 格式

上下文 ID 遵循模式：`YYYY-MM-DD_feature-name`
- 日期：YYYY-MM-DD 格式的当前日期
- 功能名称：小写英文，使用连字符
- 示例：`2026-02-01_user-authentication`

### 初始化后的下一步

1. 编辑 `requirements.md` 填写需求详情
2. 更新 `plan.md` 创建实施计划
3. 开始开发
4. 随着工作进展更新文档
5. 修改静态文档时更新 `.context.json` 元数据

## 列出活跃上下文

查看所有当前活跃的开发上下文。

### 使用脚本

```bash
# 仅列出活跃上下文
python scripts/list_contexts.py

# 列出活跃和已归档的上下文
python scripts/list_contexts.py --all
```

### 输出格式

```
📋 活跃上下文列表：

Context ID                          标题                 负责人            状态          最后更新
--------------------------------------------------------------------------------------------------------------
2026-02-01_user-authentication     用户认证功能          developer        in_progress   2026-02-01 15:30
2026-01-30_api-refactor            API重构              developer        in_progress   2026-01-30 10:00

共 2 个活跃上下文
```

### 查找上下文详情

列出上下文后，读取特定上下文的 `.context.json` 获取完整详情：
```bash
# 读取元数据
cat docs/contexts/2026-02-01_user-authentication/.context.json

# 读取特定文档
cat docs/contexts/2026-02-01_user-authentication/requirements.md
```

## 归档上下文

将上下文标记为已完成并生成归档摘要。

### 使用脚本

```bash
python scripts/archive_context.py <context-id>
```

**示例：**
```bash
python scripts/archive_context.py 2026-02-01_user-authentication
```

### 执行操作

脚本会：
1. **更新元数据**：设置 `status: "completed"` 和 `completedAt` 时间戳
2. **生成 SUMMARY.md**：创建归档摘要，包含：
   - 基本信息（日期、负责人、分支）
   - 完成状态（已实现功能、技术栈）
   - 关键决策
   - 问题和解决方案
   - 测试结果
   - 更新的静态文档
   - 后续工作
   - 经验总结
3. **更新索引**：将上下文从 `activeContexts` 移至 `archivedContexts`

### 归档后

上下文保留在 `docs/contexts/[contextId]/` 中，但状态变为 `status: "completed"`。文件不会移动，只有状态改变。

## 文档结构

### 静态文档 (`docs/static/`)

按功能模块组织的长期维护文档：
- `architecture/` - 系统架构/技术选型
- `design/` - 模块设计/数据流
- `api/` - 接口定义/协议
- `guide/` - 使用/部署手册
- `spec/` - 需求规格/验收

### 动态文档 (`docs/contexts/YYYY-MM-DD_feature/`)

**根目录概念**：
- 明确说明根目录：`docs/contexts/YYYY-MM-DD_feature/`
- 所有文档直接放在根目录，无需子目录层级

**职位相关文档**（直接放在根目录）:
- 需求规划师：`requirement.md`, `user-story.md`, `acceptance.md`, `flow.md`
- 架构师：`design.md`, `diff.md`, `sync.md`
- TDD开发工程师：`task-breakdown.md`, `test-cases.md`, `dev-log.md`, `test-report.md`
- 重构工程师：`baseline.md`, `plan.md`, `progress.md`, `comparison.md`, `summary.md`, `tests/`
- 测试工程师：`test-cases.md`, `execution-log.md`, `error-analysis.md`, `summary.md`

**完整示例**:
```
docs/contexts/YYYY-MM-DD_feature/
├── requirements.md              # 功能需求
├── requirement.md               # 需求文档（需求规划师）
├── user-story.md                # 用户故事
├── acceptance.md                # 验收标准
├── flow.md                      # 流程图
├── design.md                    # 架构设计
├── diff.md                      # 差异分析
├── sync.md                      # 同步报告
├── task-breakdown.md            # 任务拆解
├── test-cases.md                # 测试用例
├── dev-log.md                   # 开发日志
├── test-report.md               # 测试报告
├── baseline.md                  # 重构基线
├── refactor-plan.md             # 重构方案
├── progress.md                  # 进度跟踪
├── comparison.md                # 测试对比
├── tests/                       # 测试代码目录
│   ├── test_*.py
│   └── fixtures/
├── execution-log.md             # 执行日志
├── error-analysis.md            # 错误分析
├── summary.md                   # 总结文档
└── .context.json                # 元数据
```

**文档说明**：
- `requirements.md` - 功能需求文档（功能级）
- `architecture-changes.md` - 此功能的架构变更
- `feature-spec.md` - 功能规格说明
- `plan.md` - 实施计划
- `todos.md` - 任务清单
- `test-plan.md` - 测试计划
- `SUMMARY.md` - 归档摘要（归档时生成）

### 元数据文件

**`.context.json`** - 上下文元数据：
```json
{
  "contextId": "2026-02-01_user-authentication",
  "status": "in_progress",
  "createdAt": "2026-02-01T10:00:00Z",
  "updatedAt": "2026-02-01T15:30:00Z",
  "completedAt": null,
  "title": "用户认证功能",
  "description": "实现基于 JWT 的用户认证系统",
  "assignee": "developer-name",
  "gitBranch": "feature/user-auth",
  "documents": {
    "requirements": "requirements.md",
    "architectureChanges": "architecture-changes.md",
    "featureSpec": "feature-spec.md",
    "plan": "plan.md",
    "todos": "todos.md",
    "testPlan": "test-plan.md"
  },
  "staticDocsUpdated": []
}
```

**`.contexts-index.json`** - 全局索引：
```json
{
  "activeContexts": [
    {
      "contextId": "2026-02-01_user-authentication",
      "title": "用户认证功能",
      "status": "in_progress",
      "assignee": "developer-name",
      "updatedAt": "2026-02-01T15:30:00Z"
    }
  ],
  "archivedContexts": []
}
```

## 最佳实践

1. **始终检查当前上下文** - 开始工作前读取 `.contexts-index.json`
2. **及时更新元数据** - 修改文档后更新 `updatedAt`
3. **记录静态文档变更** - 更新静态文档时添加到 `staticDocsUpdated` 数组
4. **保持文档同步** - 架构变更时同时更新动态和静态文档
5. **完整归档** - 确保 SUMMARY.md 全面准确
6. **提取经验** - 将关键经验添加到项目级 CLAUDE.md

## 使用上下文工作

### 开始功能开发

1. 列出活跃上下文查看进行中的工作
2. 如果开始新工作，初始化新上下文
3. 编辑初始文档填写详情
4. 开始开发

### 开发过程中

1. 随着工作进展更新上下文文档
2. 更新静态文档时，记录在 `.context.json` 的 `staticDocsUpdated` 中
3. 更新元数据中的 `updatedAt` 时间戳
4. 保持 `plan.md` 和 `todos.md` 最新

### 完成工作

1. 确保所有文档都是最新的
2. 验证静态文档已更新
3. 运行测试并更新 `test-plan.md`
4. 归档上下文
5. 审查生成的 SUMMARY.md 并根据需要增强

## 并行开发

上下文工程系统支持多个开发者同时处理不同功能：

- 每个功能都有自己的上下文目录
- 上下文是独立的，不会相互干扰
- 索引文件跟踪所有活跃和已归档的上下文
- 状态字段区分活跃和已完成的工作

## 参考资料

完整规范和模板包含在此 skill 中：
- **规范文档**：[references/specification.md](references/specification.md) - 完整的上下文工程系统规范
- **模板文档**：[references/templates.md](references/templates.md) - 所有文档的模板格式

这些参考文档是 skill 自带的，不依赖项目中的任何文件。

## 故障排除

### 上下文已存在
如果尝试创建已存在的上下文，脚本会报错。可以：
- 使用不同的功能名称
- 或使用现有上下文

### 索引文件缺失
如果 `.contexts-index.json` 不存在，初始化第一个上下文时会自动创建。

### 元数据损坏
如果元数据文件损坏，按照上面显示的格式手动重新创建。

## 脚本参考

所有脚本位于 `scripts/` 目录：

- **init_context.py** - 初始化新开发上下文
- **list_contexts.py** - 列出活跃（和已归档）上下文
- **archive_context.py** - 归档已完成上下文

所有脚本都可以从项目内的任何目录运行 - 它们会通过查找 `docs/contexts/` 自动找到项目根目录。
