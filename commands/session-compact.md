---
name: session-compact
command-symbol: session-compact
description: 执行智能压缩并自动清理上下文
argument-hint: ""
allowed-tools: ["Task", "Bash", "Read", "Write"]
plugin: session-memory
example-call: "/session-compact"
---

执行智能压缩并自动清理上下文。调用 smart-compact-agent 分析会话历史，压缩前70%的内容，保留后30%，然后自动执行 /clear。

## 实现步骤

1. **调用 smart-compact-agent**
   - 使用 Task 工具调用 smart-compact-agent（subagent_type: "smart-compact-agent"）
   - Agent 会分析当前会话内容
   - 执行智能压缩策略：
     * 压缩前70%的消息
     * 保留后30%的消息
     * 文件内容替换为文件路径
     * 网页内容替换为链接
   - 将压缩后的记忆保存到 `.claude/session-memory.md.tmp`

2. **等待压缩完成**
   - 等待 agent 完成压缩
   - 显示压缩报告（消息数、压缩率等）

3. **自动执行 /clear**
   - 压缩完成后，告知用户即将执行 /clear
   - 使用 Bash 工具执行 `/clear` 命令（或提示用户手动执行）
   - 清理当前上下文

## 输出格式

```
🔄 开始智能压缩...

[调用 smart-compact-agent 的输出]

✓ 压缩完成
- 压缩后的记忆已保存到: .claude/session-memory.md.tmp
- 下次 session 启动时会自动加载

⚠️ 即将执行 /clear 清理当前上下文
请手动执行 /clear 命令以应用压缩
```

## 注意事项

- 压缩后的记忆会在下次 session 启动时自动加载
- /clear 命令需要用户手动执行（Claude 无法直接执行 slash 命令）
- 压缩策略：前70%压缩，后30%保留

